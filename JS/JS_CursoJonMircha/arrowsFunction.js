

const saludar = nombre => console.log(`Hola ${nombre}`);

saludar('perro')

const sumar = (a,b) => a+b


const numeros = [1,2,3,4,5]

numeros.forEach((n,pos)=>console.log(`el elemento ${n} esta en la posicion ${pos}`))

