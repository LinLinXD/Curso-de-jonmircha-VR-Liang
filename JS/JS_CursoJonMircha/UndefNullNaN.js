let indefinida; //Variable undefined por no haber sido inicializada.
let nula = null; //Variable vacia asignada por el programador.

console.log(indefinida);
console.log(nula);

let noNum = 'hola'; 
let num = 2; 

console.log(noNum*num); //Regresa un Not a Number (NAN)

